# Test documents

Test fixtures are generated in memory by `../utils/documents.ts`. They contain only synthetic content and are never production documents. Downloads are inspected using pdf-lib and JSZip instead of trusting success messages alone.
